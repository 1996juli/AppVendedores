<resources xmlns:tools="http://schemas.android.com/tools">

    //Tema principal

    <style name="Theme.AppVendedores" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">#53555A</item>
        <item name="colorPrimaryVariant">#8B8891</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    //Temas que heredan del principal (name=theme.AppVendedores + nombreqdefinimos),
    con el fin de evitar poner el parent, ya que si no marca error.
    //Temas secundarios --> ej se aplican en el manifest a una activity especifica.

    <style name="Theme.AppVendedores.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Appturnos" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">#AC60E2</item>
        <item name="colorPrimaryDark">#2196F3</item>
        <item name="colorAccent">@color/teal_200</item>
        <item name="windowActionBar">false</item>
    </style>

    //Temas para un componente de una vista en especifico , aca solo lo definimos los colores.
    Para aplicarlo al componente abrimos el layout, seleccionamos el componente ej formulario,
    y buscamos la lupita --> colocamos theme y seleccionamos la opcion que queramos, dentro de
    las opciones apareceran todos los temas de este archivo.

    <style name="Theme.AppVendedores.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.AppVendedores.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>